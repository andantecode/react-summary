specVersion: "v1beta"
serviceId: "react-summary"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "react-summary-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
